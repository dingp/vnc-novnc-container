# Defaults used by the Jupyter kernelspec wrapper.
# Values in strings are expanded from the host environment when the kernel starts.

image: ghcr.io/dingp/debian:12-vnc-novnc
userns: keep-id
keep_groups: true

volume:
  - "${SCRATCH}:${SCRATCH}:rw"
  - "${CFS}:${CFS}:rw"
